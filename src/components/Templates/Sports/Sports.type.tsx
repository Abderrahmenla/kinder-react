export interface SportsIdProps {
  slug: string;
  categoryId: number;
  sportsId: number;
  championshipId: number;
}

export interface AltenarSportsPageProps {
  sportGroupType?: string;
  championshipType?: string;
}
