import { styled } from '@mui/material/styles';

export const CategoryTitle = styled('h2')({
  display: 'inline-block',
  fontSize: '14px',
  color: 'var(--white)',
  fontWeight: '500',
  paddingLeft: '8px',
  lineHeight: '1'
});
