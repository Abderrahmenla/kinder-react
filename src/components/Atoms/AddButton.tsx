import { styled } from '@mui/material';

export const AddButton = styled('div')({
  boxSizing: 'border-box',
  width: '58px',
  height: '58px',
  background: 'linear-gradient(90deg, var(--bluish-violet) 0%, var(--pure-blue) 100%)',
  borderRadius: '50%',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center'
});
