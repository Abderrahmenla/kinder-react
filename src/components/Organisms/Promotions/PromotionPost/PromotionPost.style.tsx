import { styled } from '@mui/material/styles';

export const PromotionPostWrapper = styled('div')({
  a: {
    textDecoration: 'none'
  }
});

export const PromotionPostContainer = styled('div')({
  display: 'flex',
  flexDirection: 'column',
  width: '100%',
  borderRadius: '15px',
  background: 'var(--very-dark-violet-200)'
});
