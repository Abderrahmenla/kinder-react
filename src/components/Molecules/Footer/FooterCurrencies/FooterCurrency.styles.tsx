import Typography from '@/components/Atoms/Typography/Typography';
import styled from '@emotion/styled';

export const BitcoinTextWrapper = styled('div')({
  marginLeft: '16px'
});

export const BitcoinText = styled(Typography)({
  color: 'var(--white)'
});
