import { styled } from '@mui/material/styles';

export const RewardEmptyStateContainer = styled('div')({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  flexDirection: 'column',
  margin: '5vh 0vw 0vh 0vw'
});
